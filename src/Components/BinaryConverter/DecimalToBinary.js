
/**
 * 
 * @param {int} decimal The number to be converted to binary.
 * @returns The binary value of the decimal passed in.
 */
const convertNumberToBinary = (decimal) => {
    const binaryValue = decimal.toString(2);
    return binaryValue;
}

const convertTextToASCII = (text) => {

}

const convertASCIIToBinary = (ascii) => {
    
}

export default convertNumberToBinary;